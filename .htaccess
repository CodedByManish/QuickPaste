RewriteEngine On

# Allow existing files/folders
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Handle short URLs
RewriteRule ^([a-zA-Z0-9]+)$ php/s/index.php?id=$1 [L,QSA]
